export default function App(): React.JSX.Element {
  return (
    <main className="app-shell">
      <section className="status-card" aria-labelledby="app-title">
        <h1 id="app-title">P2P Server</h1>
        <p>Aplicação iniciada com sucesso.</p>
      </section>
    </main>
  )
}

